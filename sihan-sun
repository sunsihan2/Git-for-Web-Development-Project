first project html
https://codepen.io/sunsihan2/pen/dypQOPw

second project css
https://codepen.io/sunsihan2/pen/MWjzbyE

third project
https://codepen.io/sunsihan2/pen/ExgGvbN


    1. What is Semantic HTML? 
        A semantic element clearly describes its meaning to both the browser and the developer.
        ex. <form>, <table>, <article>

    2. What is HTML used for? 
        HTML used to structure a web page and its content.

    3. What is an attribute and where do we put it? 
        Attributes define additional characteristics or properties of the element such as width and height of an image.
        We put in the HTML.

    4. What is the h1 tag used for? How many times should I use it on a page?
       H1 tag indicates the most important heading on a website. Use only one H1 tag per page. 

    5. Name two tags that have required attributes
        <img>  <a href>

    6. What do we put in the head of our HTML document? 
        <meta>, <title>

    7. What is an id? 
        The id attribute specifies a unique id for an HTML element.The id attribute used to point to a style in a style sheet,
        anc by JavaScript to manipulate the element with the specific id.

    8. What elements can I add an id to?
        Most of tags can add id attribute.

    9. How many times can I use the same id on a page? 
        Id should only be used once on a single document.

    10. What is a class? 
        The class is an attribute which specifies one or more elements used by CSS and JavaScript.

    11. What elements can I add a class to? 
        Every tags can add a class.

    12. How many times can I use the same class on a page? 
        As many as you want.

    13. How do I get my link to open in a new tab?
        set target = "_blank"

    14. What is the alt attribute in the image tag used for? 
        The alt attribute specifies an alternate text for an image.

    15. How do I reference an id?
        #idName {}

    16. What is the difference between a section and a div
        The section tag defines section in a document like chapters, headers, footers whereas div tag defines a division in an HTML document.
        <section> means that the content inside is grouped and should appear as a entry in an outline of a page whereas <div> doesn't convey any meaning, aside from any found in its class, lang and title attributes.

    17. What is CSS used for? 
        CSS is the language for describing the presentation of Web pages, including colors, layout, and fonts.

    18. How to we select an element? Example - every h2 on the page
        h2 is selected by less important than h1, but more important than h3.

    19. What is the difference between a class and an id? - Give me an example of when I might use each one
        Id only can uses once, but class can uses many time. 
        When you only have one introduction then uses id. If you have multiple introductions, you should use class.

    20. How do we select classes in CSS?
        .className{}

    21. How do we select a p element with a single class of “human””?
        .human p{ }

    22. What is a parent child selector? When would this be useful? 
        Parent child selector is nested selector and it takes decendent order. When you want your child selector more specific, it is useful.

    23. How do you select all links within a div with the class of sidebar?
        .sidebar a: link {}

    24. What is a pseudo selector?
        A pseudo-class is used to define a special state of an element.

    25. What do we use the change the spacing between lines?
        line-height

    26. What do we use to change the spacing between letters?
        letter-spacing: 3px;

    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
        text-transform: capitalize;
        text-transform: lowercase;
        text-transform: uppercase;

    28. How do I add a 1px border around my div that is dotted and black?
        border: 20px dotted black;

    29. How do I select everything on the page? 
        * { }

    30. How do I write a comment in CSS?
        //

    31. How do I find out what file I am in, when I am using the command line? 
        pwd

    32. Using the command line - how do I see a list of files/folders in my current folder?
        ls

    33. How do I remove a file via the command line? Why do I have to be careful with this? 
        rm    Once it remove, you will loose it forever.

    34. Why should I use version control? 
        Version control is important to keep track of changes, and keep every team member working off the latest versioon.
        This helps team collaborate around the world.

    35. How often should I commit to github?
        It depends. For my own project, i can commit anytime. One of the advantages of committing is sharing code between computer systems.

    36. What is the command we would use to push our repo up to github? 
        git push -u origin fileName

    37. Walk me through Lambda's git flow. 
        First fork the project.
        Then clone your repo address
        git clone <your repo address>
        make a branch  cd location
                       git checkout -b branchName
        Push your new branch up to GitHub and make all future pushes to that branch :  git push -u origin name
        compare & pull request on github.
    

Stretch Questions

    1. What is the difference between an inline element and a block element?
        A block element will always creat a new line after the closing tag, whereas an inline element will not.
        
    2. What happens when an element is positioned absolutely?
        An absolutely positioned element no longer exists in the normal document layout flow.

    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 


    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
        black element: <address> <article> <header>
        inline element: <span>  <div>
        inline-block by default: button

    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? 
        box is a sized place where we want to put elements in. 
        *{
            box-sizing: border-box;
        }